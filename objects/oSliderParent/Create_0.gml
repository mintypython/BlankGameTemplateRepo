// Initialize a variable to pass text through as a string
buttonText = ""

// Initialize variable for controller navigation
selected = 0

// Run only once flag
runOnce = 0

// Check if the button is hovered (MOUSE)
hovering = 0